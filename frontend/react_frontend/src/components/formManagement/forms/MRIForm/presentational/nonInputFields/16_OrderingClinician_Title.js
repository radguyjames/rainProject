import React from "react";

// Styles
import { Typography, Box } from "@material-ui/core";

export const OrderingClinician_Title = () => {
  return (
    <Box px={1}>
      <Typography style={{ fontWeight: "bold", fontSize: "13.4px" }}>
        ORDERING CLINICIAN
      </Typography>
    </Box>
  );
};
